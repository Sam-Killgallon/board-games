<div class="banner-small flex">
  <div class="m-auto">
    <div class="banner-title">New Game</div>
  </div>
</div>
<div class="card card-floating text-center mx-auto min-h-300">
  <div class="card__body"></div>
</div>

<%= label_tag 'q', 'Search' %>
<%= search_field_tag 'q', nil, data: { url: search_games_path, remote: true } %>

<div id="games">Search for a game to add</div>

<%= link_to 'Back', games_path %>
